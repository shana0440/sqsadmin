apiVersion: v1
kind: Service
metadata:
  name: {{ include "sqsadmin.fullname" . }}
  labels:
    {{- include "sqsadmin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sqsadmin.selectorLabels" . | nindent 4 }}
